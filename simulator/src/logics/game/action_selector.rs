use crate::models::player::Player;

pub fn select_action(player: Player, action: &str) {
    // Tke the position of the player,
    // Return probabilities map for this position
    // Adjust the probabilities for select one action
    // Calculate if this action success or fail using the probabilities
    // Return the new action or the result of the calcs.
}